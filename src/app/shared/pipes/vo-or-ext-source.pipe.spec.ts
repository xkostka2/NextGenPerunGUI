import { VoOrExtSourcePipe } from './vo-or-ext-source.pipe';

describe('VoOrExtSourcePipe', () => {
  it('create an instance', () => {
    const pipe = new VoOrExtSourcePipe();
    expect(pipe).toBeTruthy();
  });
});
